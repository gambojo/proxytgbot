networks:
  bot_network:
    driver: bridge

services:
  bot:
    build: .
    container_name: bot
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - bot_network
#    depends_on:
#      postgres:
#        condition: service_healthy

#  adminer:
#    image: adminer
#    container_name: bot_adminer
#    ports:
#      - "48080:8080"
#    networks:
#      - bot_network
#    depends_on:
#      - postgres
#
#  postgres:
#    image: postgres:15
#    container_name: bot_postgres
#    env_file:
#      - .env
#    ports:
#      - "45432:5432"
#    volumes:
#      - postgres_data:/var/lib/postgresql/data
#      - ./databases/init/postgres-init.sql:/docker-entrypoint-initdb.d/01-init.sql
#    networks:
#      - bot_network
#    healthcheck:
#      test: ["CMD-SHELL", "pg_isready -d proxy_bot_db -U proxy_bot_user"]
#      interval: 10s
#      timeout: 5s
#      retries: 5
#
#volumes:
#  postgres_data:
#    driver: local
